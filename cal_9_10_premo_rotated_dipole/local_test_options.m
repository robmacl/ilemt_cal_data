%% local test definitions, included by test_script.m

% In this directory we have just axis sweep data, so calibration comes from
% elsewhere. 
cal_runs = {};

% Options are set up in local_check_options.
cp_opts = {};
cp_runs = {
    {'../cal_9_1_premo_rotated_dipole/output/XYZ_hr_cal.mat'}
    };
